/**
 * Back-compat shim. Canonical implementation lives in src/components/shared/ClientStatusTimeline.tsx
 * Kept so existing imports under the wl-proposal-public namespace keep working.
 */
export { ClientStatusTimeline as WLClientStatusTimeline } from '@/components/shared/ClientStatusTimeline';
